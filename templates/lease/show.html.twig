{% extends 'layouts/app.html.twig' %}

{% block title %}Lease{% endblock %}

{% block content %}
    <h1>Lease</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lease.id }}</td>
            </tr>
            <tr>
                <th>StartDate</th>
                <td>{{ lease.startDate ? lease.startDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>EndDate</th>
                <td>{{ lease.endDate ? lease.endDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('lease_index') }}">back to list</a>

    <a href="{{ path('lease_edit', {'id': lease.id}) }}">edit</a>

    {{ include('lease/_delete_form.html.twig') }}
{% endblock %}
